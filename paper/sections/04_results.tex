\section{Results}
\label{sec:results}
% Every number below comes from generated files. Write the sentences AFTER reading results/tables/REPORT.md,
% and state each verdict exactly as the mechanical rule produced it (including inconclusive/falsified).
\subsection{Compliance (H1) and cost of compliance (H2, H2b)}
\genTable{tab_e1_emu}{E1 (emulation): round latency (ms, pooled percentiles with 95\% cluster-bootstrap CI), round success, policy violations and payload per round.}{tab:e1emu}
\genTable{tab_ratios_emu}{Policy+Resource vs.\ baselines (emulation): paired $p_{50}$/$p_{99}$ ratios with 95\% CI and effect sizes.}{tab:ratios}
\genFig{fig_e1_violations_emu}{Policy violations per round by ineligible fraction and strategy (emulation).}{fig:viol}
[H1: \textbf{verdict from Table~\ref{tab:hyp}}. The policy-blind baselines violated policy in [X] of [N] rounds at $f{=}0.25$.
H2 at $f{=}0.25$: ratio \HTwoRatioEmu\ (95\% CI \HTwoRatioCIEmu) $\Rightarrow$ [supported/falsified/inconclusive]. H2b at $f{=}0.50$: [\ldots].]

\subsection{Degradation under DDIL conditions (H3)}
\genFig{fig_e2_p99_emu}{$p_{99}$ round latency vs.\ packet loss, added delay and bandwidth cap (emulation; 95\% CI).}{fig:e2}
\genTable{tab_e2_emu}{E2 (emulation): all degradation conditions.}{tab:e2emu}
[Describe the curve shape; report where policy-aware and resource-only diverge from random; report every condition, including those that do not support H3.]

\subsection{Failure: compliance versus availability (E3, exploratory)}
\genTable{tab_e3_emu}{E3 (emulation): eligible nodes down.}{tab:e3emu}
[Report success rate and violations per strategy when eligible-up $<$ minimum participants; this is the cost of never violating policy.]

\subsection{Baseline architectures}
\genTable{tab_substrates}{Same workload on each substrate (E1, $f{=}0.25$, no impairment).}{tab:substrates}
\genTable{tab_centralized}{Centralised single-node floor: ineligible institutions' raw data pooled by construction.}{tab:central}

\subsection{Simulation versus production}
\genTable{tab_trace}{Real-link trace logged during the production trial (not synthesised).}{tab:trace}
\genTable{tab_sim_vs_prod}{Headline quantities across tiers; ``reproduces'' = same direction and within $\pm$20\%.}{tab:svp}
\genTable{tab_hypotheses_emu}{Hypothesis verdicts (emulation) by the pre-registered rules.}{tab:hyp}
[State, for each row of Table~\ref{tab:svp}, whether it reproduces; for each that does not, give the mechanism.]
