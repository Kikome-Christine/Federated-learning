# Placeholder tags. Run `make pin` (needs Docker + network) to replace these
# with immutable @sha256 digests, then commit the result.
RUST_IMAGE=rust:1.90-bookworm
RUNTIME_IMAGE=debian:bookworm-slim
OPA_IMAGE=openpolicyagent/opa:1.4.2
